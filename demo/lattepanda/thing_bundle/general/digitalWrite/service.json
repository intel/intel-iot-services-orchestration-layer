{
  "description":"write true or false to gpio pin",
  "spec": {
    "in": {
      "ports": [{
        "name": "on",
        "type": "boolean"
        }]
    },
    "out": {
      "ports": [{
        "name": "status",
        "type": "boolean"
      }]
    },
    "config": [
      {
        "name": "pin",
        "type": "int",
        "required": true
      }
    ]
  }
}